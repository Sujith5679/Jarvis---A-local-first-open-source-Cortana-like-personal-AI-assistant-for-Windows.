# --- Phase 0: core / config / storage / logging ---
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0
PySide6>=6.6

# --- Phase 1: LLM providers + agent orchestration ---
httpx>=0.27
langgraph>=0.1
langchain-core>=0.2

# --- Phase 2: document parsing + retrieval ---
PyMuPDF>=1.24
python-docx>=1.1
python-pptx>=0.6.23
openpyxl>=3.1
beautifulsoup4>=4.12
faiss-cpu>=1.8
sentence-transformers>=2.7

# --- Phase 3: scheduling ---
APScheduler>=3.10

# --- Phase 4: web (readability-style boilerplate stripping for fetched pages;
#     local HTML files use plain BeautifulSoup tag-stripping, see documents/text.py) ---
readability-lxml>=0.8
markdown>=3.6  # renders LLM markdown (tables, headers, code blocks) in chat bubbles

# --- Phase 5: voice (fully local/offline STT + TTS) ---
faster-whisper>=1.0
sounddevice>=0.5
numpy>=1.26
piper-tts>=1.3
tzdata>=2024.1  # Windows doesn't ship IANA tz data; needed by some faster-whisper/onnx deps

# --- Phase 6: Windows integration (tray icon uses PySide6's QSystemTrayIcon,
#     already a dependency; Task Scheduler is driven via stdlib subprocess) ---
keyboard>=0.13.5  # global hotkey (ui/hotkey.py)


# --- Dev / test tooling (all phases) ---
pytest>=8.0
pytest-asyncio>=0.23
ruff>=0.4
mypy>=1.10
